export interface Photo {
  id: string;
  url: string;
  name: string;
  filename: string;
  category: string;
}

export interface Video {
  id: string;
  title: string;
  description: string;
  vimeoUrl: string;
  category: string;
}